FACEFORGE BDO 0.3.0 - CI SMOKE STABILITY FIX

1. Extract this ZIP directly over the current FaceForge BDO 0.3.0 - SOURCE folder.
2. Allow Windows to replace the included files.
3. Keep the existing .git folder.
4. Double-click PUSH_CI_SMOKE_FIX.bat.

The v0.3.0 release has already been created. This patch only updates the push-triggered
CI smoke check so temporary WebView2 profile cleanup cannot falsely fail a successful
native window start/stop test.
