FACEFORGE BDO 0.3.0 - WORKFLOW REGISTRATION FIX

1. Extract this ZIP directly over your existing FaceForge BDO 0.3.0 - SOURCE folder.
2. Allow Windows to replace the included files.
3. Keep the existing .git folder.
4. Double-click RETRY_RELEASE_NOW.bat.

The repaired publisher now:
- pushes main first;
- sets and verifies main as the GitHub default branch;
- waits for GitHub to index .github/workflows/release.yml;
- retries transient workflow-dispatch 404 errors;
- watches the release build and prints failed logs automatically.
